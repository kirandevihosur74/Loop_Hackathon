import { Screen } from "@/components/layout/Screen";

// Route shell — replaced by the Stats screen build.
export default function StatsPage() {
  return (
    <Screen>
      <h1 className="text-xl font-bold text-ink">Stats</h1>
      <p className="mt-1 text-sm text-sub">Smart electricity viz — coming together.</p>
    </Screen>
  );
}
