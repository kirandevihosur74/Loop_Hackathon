import { Screen } from "@/components/layout/Screen";

// Route shell — replaced by the Schedule screen build.
export default function SchedulePage() {
  return (
    <Screen>
      <h1 className="text-xl font-bold text-ink">Schedule</h1>
      <p className="mt-1 text-sm text-sub">The agent&apos;s plan — coming together.</p>
    </Screen>
  );
}
