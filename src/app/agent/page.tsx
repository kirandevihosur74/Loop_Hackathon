import { Screen } from "@/components/layout/Screen";

// Route shell — replaced by the Agent screen build.
export default function AgentPage() {
  return (
    <Screen>
      <h1 className="text-xl font-bold text-ink">Agent</h1>
      <p className="mt-1 text-sm text-sub">The loop, made visible — coming together.</p>
    </Screen>
  );
}
